version: "3.9"

services:
  db:
    image: postgres:16
    container_name: email-postgres
    restart: unless-stopped
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: email
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user -d email"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  pgdata: